{
  "name": "myhmm-serverless",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "bundle-class-assignment": "esbuild dist/event-handlers/class-assignment/handleClassAssignmentEvent.js --bundle --platform=node --outfile=dist/event-handlers/class-assignment/handleClassAssignmentEventBundle.js",
    "upload-class-assignment": "aws s3 cp lambda/handleClassAssignmentEvent.zip s3://myhmm-serverless-bucket/handleClassAssignmentEvent.zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.25.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.782.0",
    "@aws-sdk/lib-dynamodb": "^3.782.0",
    "@aws-sdk/util-dynamodb": "^3.782.0",
    "@types/aws-lambda": "^8.10.148",
    "aws-lambda": "^1.0.7"
  }
}
